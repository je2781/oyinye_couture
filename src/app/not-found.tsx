'use client';

export default function NotFound(){
    return (
        <html>
            <body>
                <main className="flex items-center justify-center h-screen max-w-full w-full">
                    <h1 className="font-semibold text-lg">404. Page doesn&apos;t exist</h1>
                </main>
                
            </body>
        </html>
    );
}